---
// Image upload area with drag-and-drop support
---

<div id="upload-area" class="sp-upload-area">
  <input
    type="file"
    id="file-input"
    accept="image/jpeg,image/png,image/webp,image/gif"
    class="hidden"
  />
  <div class="sp-upload-content">
    <svg class="sp-upload-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        stroke-width="1.5"
        d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12"
      ></path>
    </svg>
    <div>
      <div>
        <button type="button" class="sp-upload-btn">
          <span class="sp-upload-link">Click to upload</span>
          <span class="sp-upload-text"> or drag and drop</span>
        </button>
      </div>
      <p class="sp-upload-hint">JPEG, PNG, WebP, or GIF (max 50MB)</p>
    </div>
  </div>
  <div id="file-info" class="sp-file-info hidden"></div>
  <div id="validation-message" class="hidden"></div>
</div>

<style>
  .sp-upload-area {
    background: var(--sp-bg-card);
    border: 2px dashed var(--sp-border);
    border-radius: var(--sp-radius-xl);
    padding: 3rem 2rem;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
    box-shadow: var(--sp-shadow);
  }

  .sp-upload-area:hover {
    border-color: var(--sp-lavender);
  }

  .sp-upload-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .sp-upload-icon {
    width: 3.5rem;
    height: 3.5rem;
    color: var(--sp-text-soft);
  }

  .sp-upload-btn {
    cursor: pointer;
    background: transparent;
    border: 0;
    padding: 0;
    font-family: var(--sp-font-body);
    font-size: 1rem;
  }

  .sp-upload-link {
    color: var(--sp-coral);
    font-weight: 600;
  }

  .sp-upload-link:hover {
    color: var(--sp-coral-dark);
  }

  .sp-upload-text {
    color: var(--sp-text-muted);
  }

  .sp-upload-hint {
    font-size: 0.85rem;
    color: var(--sp-text-soft);
    margin-top: 0.35rem;
  }

  .sp-file-info {
    margin-top: 1rem;
    font-size: 0.875rem;
    color: var(--sp-text-muted);
  }
</style>
