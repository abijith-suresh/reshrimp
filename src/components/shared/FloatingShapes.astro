---
// Reusable floating background blobs for marketing pages
---

<div class="pop-shapes" aria-hidden="true">
  <div class="pop-shape shape-1"></div>
  <div class="pop-shape shape-2"></div>
  <div class="pop-shape shape-3"></div>
  <div class="pop-shape shape-4"></div>
  <div class="pop-shape shape-5"></div>
</div>

<style>
  .pop-shapes {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .pop-shape {
    position: absolute;
    border-radius: 50%;
    opacity: 0.4;
    will-change: border-radius, transform;
  }

  .shape-1 {
    width: 200px;
    height: 200px;
    background: var(--sp-coral-light);
    top: 10%;
    right: 5%;
    border-radius: 30% 70% 70% 30% / 30% 30% 70% 70%;
    animation: popPulse 15s ease-in-out infinite;
  }

  .shape-2 {
    width: 150px;
    height: 150px;
    background: var(--sp-mint-light);
    top: 60%;
    left: 3%;
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
    animation: popPulse 18s ease-in-out infinite reverse;
  }

  .shape-3 {
    width: 100px;
    height: 100px;
    background: var(--sp-lavender-light);
    top: 30%;
    left: 15%;
    border-radius: 40% 60% 60% 40% / 70% 30% 70% 30%;
    animation: popPulse 12s ease-in-out infinite;
  }

  .shape-4 {
    width: 180px;
    height: 180px;
    background: var(--sp-yellow-light);
    bottom: 15%;
    right: 10%;
    border-radius: 70% 30% 50% 50% / 30% 50% 50% 70%;
    animation: popPulse 20s ease-in-out infinite reverse;
  }

  .shape-5 {
    width: 120px;
    height: 120px;
    background: var(--sp-border-light);
    top: 75%;
    left: 45%;
    border-radius: 50% 50% 30% 70% / 60% 40% 60% 40%;
    animation: popPulse 14s ease-in-out infinite;
  }

  @keyframes popPulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.7;
    }
    50% {
      transform: scale(1.12);
      opacity: 1;
    }
  }
</style>
