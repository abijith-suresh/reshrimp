---

---

<div class="marketing-tech-grid">
  <slot />
</div>

<style>
  .marketing-tech-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 0 var(--space-8);
    margin-top: var(--space-6);
  }

  @media (max-width: 560px) {
    .marketing-tech-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
